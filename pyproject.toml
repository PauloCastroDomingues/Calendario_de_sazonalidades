[project]
name = "calendario-reise"
version = "0.5.31"
requires-python = ">=3.12,<3.13"
dependencies = [
  "fastapi>=0.110,<1",
  "uvicorn[standard]>=0.27,<1",
  "google-cloud-bigquery>=3.25,<4",
]

[tool.vercel]
entrypoint = "backend.app:app"
